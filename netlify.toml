[build]
  publish = "."
  # Production branch: dev (set in Netlify dashboard at Site > Deploys > Continuous Deployment)
  # Auto-deploys from dev on every git push. index.html is the entry point.
  # main is the clean submission branch and carries no rendered site.
